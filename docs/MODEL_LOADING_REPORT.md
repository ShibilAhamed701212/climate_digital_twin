# Model Loading Report

- Selected production model: `lstm-real-v2`.
- Registry status: `REAL` and `VALIDATED`.
- Checkpoint loads successfully from `models/checkpoints/lstm-real-v2_best.pt`.
- Feature and target scalers load successfully.
- Inference executes with PyTorch 2.13.0 CPU wheels.
- Prediction provenance includes model ID, confidence, forecast ID, training run, dataset ID, and authenticity.
- No missing or corrupted checkpoint was observed.
