# Learned EO checkpoints (not committed)

Place official weights here or set `MODEL_WEIGHTS_DIR`.

```
flood/unet/model.pt
change/changeformer/*.pt
```

Download with `python scripts/download_eo_models.py`.
Default flood mapper remains `MODEL_FLOOD=threshold`.
